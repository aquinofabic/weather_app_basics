import requests  # module allows to send HTTP requests over to python

api_key = '78c6bcb4a66344859f80385138fe0a20'

user_input = input("Enter city: ")

weather_data = requests.get{"https://qpi.openweathermap.org/data/2.5/weather?q={user_input}&units=metric&APPID={api_key"}

#print(weather_data.json())  check weather data works
#json() converts to a human-readable format - converts data structures into JSON strings


if weather_data.son()['cod'] == '404':  #'cod' is the status code that the API returns.
    print("No City Found")
else:
    weather = weather_data.json()['weather'][0]['main']  #'main' key is in the first index of the weather array. Array -> first index [0] -> 'main' key
    
    temp = weather_data.json()['main']['temp']  #'main' key not in the weather array. 'temp' key. 'main' key -> 'temp' key
    
    print(f"The weather in {user_input} is: {weather}")
    print(f"The temperature in {user_input} is: {temp}°C")

#f-strings provide a concise, convenient way to embed python expressions inside string literals for formatting

# now input the case where there are no cities for the user input. Line 13.

#remember to do pip install requests in terminal
